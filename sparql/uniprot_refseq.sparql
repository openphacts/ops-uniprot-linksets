PREFIX uniprot: <http://purl.uniprot.org/core/>
CONSTRUCT {
	?uniprot_uri rdfs:seeAlso ?refseq_uri
} WHERE {
	GRAPH <http://purl.uniprot.org> {
		?uniprot_uri rdfs:seeAlso ?refseq_uri .
		?refseq_uri uniprot:database <http://purl.uniprot.org/database/RefSeq> .
	}
}
